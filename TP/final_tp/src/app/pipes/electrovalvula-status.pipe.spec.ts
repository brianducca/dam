import { ElectrovalvulaStatusPipe } from './electrovalvula-status.pipe';

describe('ElectrovalvulaStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ElectrovalvulaStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
