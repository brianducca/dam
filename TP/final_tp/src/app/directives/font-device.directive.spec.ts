import { FontDeviceDirective } from './font-device.directive';

describe('FontDeviceDirective', () => {
  it('should create an instance', () => {
    const directive = new FontDeviceDirective();
    expect(directive).toBeTruthy();
  });
});
